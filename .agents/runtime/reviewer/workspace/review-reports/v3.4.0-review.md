# Review Report: v3.4.0 (T-038~T-044)

**Commit**: d0e9cea
**Reviewer**: code-review agent
**Date**: 2025-07-17
**Decision**: 🟠 REQUEST_CHANGES

## Summary

11 issues found: 2 CRITICAL, 3 HIGH, 6 MEDIUM. Zero LOW.

## 🔴 CRITICAL (Must Fix)

### C1: Command Injection in local-html.sh — Python string embedding
- **File**: `scripts/hitl-adapters/local-html.sh:42`
- `$role` embedded directly in python3 -c string → arbitrary code execution
- **Fix**: Pass as sys.argv: `python3 -c "...sys.argv[1]..." "$role"`

### C2: Code Injection in local-html.sh — Triple-quote escape
- **File**: `scripts/hitl-adapters/local-html.sh:58-59`
- `${content_html}` in Python `'''...'''` → content with `'''` breaks out
- **Fix**: Write content to temp file, read in Python

## 🟠 HIGH (Should Fix)

### H1: JSON Injection in confluence.sh
- `$title` and `$content_html` unescaped in heredoc JSON
- **Fix**: Use `jq -n --arg` to construct JSON safely

### H2: XSS in review-page.html
- `{{TASK_ID}}` and `{{ROLE}}` unescaped in JavaScript strings
- **Fix**: Escape single quotes in sed substitution

### H3: Path Traversal in all HITL adapters
- No validation that task_id/role are safe for file paths
- **Fix**: Validate `[[ "$task_id" =~ ^T-[0-9]+$ ]]`

## 🟡 MEDIUM (Recommended)

### M1: Stale 3-phase refs in agent-hooks, agent-orchestrator
- `workflow_mode`, `dispatch_3phase_step()` etc. still referenced
- **Fix**: Update/deprecate remaining references

### M2: Skill count in verify-install.sh
- Says 20, list has 20 entries including agent-hitl-gate — actually correct ✅
- **Note**: False alarm — 19→20 is right (hitl-gate added, total 20)

### M3: Indirect variable expansion in confluence.sh
- `${!CONFLUENCE_TOKEN}` could read arbitrary env vars
- **Fix**: Explicit env: prefix handling

### M4: Silent failure in adapter API calls
- Failed gh/curl returns empty, agent doesn't know
- **Fix**: Exit with error on failure

### M5: HITL publish race condition
- Writes hitl_status without optimistic locking
- **Fix**: Check version before write

### M6: DFMEA guard only checks existence
- Doesn't validate RPN>100 items are mitigated
- **Fix**: Parse content, check status of high-RPN items

## Verdict

Must fix C1, C2 (security) before release. H1-H3 strongly recommended.
M1-M6 can be addressed in follow-up.
